# 问题

为什么修改`frameworks/base/core/java/android/widget/TextView.java`这个文件就可以给第三方显示作假呢？这是我看了修改之后的问题。

# Browser2

`packages/apps/`，这个路径下有非常多的原生app，我就从原生app入手学习，为什么修改`TextView.java`这个文件中的逻辑就能给第三方应用显示作假？

看到一个app或者一个模块，首先最好是看一下它的清单文件`AndroidManifest.xml`，找一下应用的入口。这个app的主入口很明显就是`WebViewBrowserActivity.java`

```xml
<activity
            android:name="org.chromium.webview_shell.WebViewBrowserActivity"
            android:label="@string/title_activity_browser"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> 
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Catch intents which do not specify a MIME type -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
            <!-- Catch intents which do specify a MIME type -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:mimeType="text/html"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="application/xhtml+xml"/>
                <data android:mimeType="application/vnd.wap.xhtml+xml"/> <!-- XHTML MP -->
            </intent-filter>
</activity>
```

