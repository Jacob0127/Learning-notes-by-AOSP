MTK14 修改了xml文件 怎么进行单编
首先 使用ms <模块名> 进行单编apk，发现修改没有生效

然后使用反编译，将apk反编译，查看源代码，发现修改的xml文件并没有被修改，还是原来的文件

所以，不能使用ms <模块名>的单编apk方式进行编译

解决方法：
使用mmm单编方式

首先，进入单编环境
其次，使用mmm + 需要编译的模块目录：模块名

但是这时候有可能会报错
删掉报错文件（vf_merged..)重新编译即可