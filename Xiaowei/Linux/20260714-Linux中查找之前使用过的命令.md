# history

可以使用history命令显示所有历史记录。

```
history
```

也可以结合grep命令搜索特定的命令

```
history | grep 关键词
```

------

# 快捷键Ctrl+R

1. 按下 Ctrl + R 进入反向搜索模式（reverse-i-search）。
2. 输入关键词，系统会实时显示匹配的历史命令。
3. 如果找到目标命令，按 *Enter* 执行；若不是目标命令，可继续按 *Ctrl + R* 查看下一个匹配项。