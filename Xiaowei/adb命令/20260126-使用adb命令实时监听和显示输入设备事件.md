`adb shell getevent -l` 是一个**Android调试命令**，用于**实时监听和显示输入设备事件**。

# 基本格式：

```bash
adb shell getevent [-选项] [设备路径]
```

## 常用选项：

- `-l`：**以人类可读的格式显示事件**（显示事件名称而非数字代码）
- `-t`：显示时间戳
- `-c [数量]`：只显示指定数量的事件后退出
- `-q`：静默模式，不显示事件
- `-p`：显示输入设备的能力信息
- `-i`：显示所有输入设备信息
- `-d`：只显示设备信息，不显示事件