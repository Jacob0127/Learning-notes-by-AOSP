# 问题

在Java中，字面量类型会直接显示的定义出来，类似于这样

```java
int a;
String str;
```

而在Python中，字面量的类型并不会显示的被定义出来，那我们需要如何知道字面量的类型呢？

```python
a = 10
b = "Hello World"
```

1. 通过`type()`函数来获取

2. 语法：

```
type(要查看的字面量)
```

