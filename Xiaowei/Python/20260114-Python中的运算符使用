# Java

在Java语言中，运算符有加`+`、减`-`、乘`*`、除`/`、取余`%`这几种

# Python

而在Python中，跟Java不同的地方在，它的多了除法（分为整除`//`和不整除`/`）、和幂指数`**`

## 除法

Python中`/`这个符号，会有小数点，不会整除，而java中会整除

```Python
print("10 / 4 = ", 10 / 4) 
# Python结果为 2.5
# Java结果为 2
```

Python中多了`//`来表示整除，对应Java中的`/`，也就是说，如果想要整除，就需要用这个`//`运算符而不是`/`

```python
print("10 // 4 = ", 10 // 4) 
# Python结果为 2.5
# Java结果为 2
```

